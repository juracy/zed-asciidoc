= Zed AsciiDoc

This extension adds support for the link:https://asciidoc.org/[AsciiDoc] language for Zed.

It is based on link:https://github.com/louiss0/tree-sitter-asciidoc/[tree-sitter-asciidoc].

== Features

- Full syntax highlighting for AsciiDoc documents
- Automatic indentation support for blocks and lists
- Text object support for vim-style operations (e.g., `dib` for delete inner block)
- Code injection support for embedded code blocks
- Proper line comment handling

== Development

=== Project Structure

----
zed-asciidoc/
├── languages/
│   └── asciidoc/
│       ├── config.toml         # Language configuration
│       ├── highlights.scm      # Syntax highlighting rules
│       ├── injections.scm      # Grammar injections for code blocks
│       ├── indents.scm         # Auto-indentation rules
│       └── textobjects.scm     # Text object definitions
├── priv/                       # Private/internal files
├── snippets/
│   └── asciidoc.json           # Snippets
├── extension.toml              # Extension configuration
├── LICENSE                     # License file
└── README.adoc                 # This file
----

=== Contributing

When contributing changes:

1. Make changes to the appropriate `.scm` files in `languages/asciidoc/`:
   - `highlights.scm` for syntax highlighting rules
   - `injections.scm` for code injection and embedded language support
   - `indents.scm` for automatic indentation behavior
   - `textobjects.scm` for vim-style text object support
2. Verify changes manually in Zed
3. Submit a pull request

=== Snippets

Some snippets were adapted from link:https://github.com/asciidoctor/asciidoctor-vscode[asciidoc vscode extension], thanks!

.Adapted Snippets
|===
| Prefix | Description

| checklist | Insert checklist
| example | Insert example block
| footnote | Insert footnote
| footnote-label | Insert footnote with label
| image | Insert image block
| image-block | Insert image inline
| include | Insert include statement
| link | Insert link
| link-simple | Insert simple link
| literal | Insert literal block
| mailto | Insert mailto link
| ordered list | Insert ordered list
| quote | Insert quote block
| sidebar | Insert sidebar block
| source | Insert source block
| table | Insert table
| tag | Insert tagged block
| unordered list | Insert unordered list
| video | Insert video block
|===

But new snippets are necessary...

.New Snippets
|===
| Prefix | Description

| line-through | Insert a line-through text
| overline | Insert an overline text
| underline | Insert an underline text
|===

== License

See link:LICENSE[LICENSE] file for details.
